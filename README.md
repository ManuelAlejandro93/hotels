<!-- Pagina que contiene información sobre diferentes
Hoteles en 4 paises de latinoamerica. -->

<!-- 
0. Se habilitó el filtro: hoteles por precios.

1. Se habilitó el filtro: el filtro de fechas de entrada y salida.

2. Se habilitó el botón clear.

3. Los filtros de fechas de entrada y salida (CheckIn y CheckOut), una vez completado el campo checkout: Cambian la fecha mostrada en el texto dinámico del header.

4. el filtro de precios de hotel, renderiza los hoteles según sea su el criterio de busqueda.

5. La API de hoteles contiene fechas de CheckIn y CheckOut muy antiguas. Por ello se anexa dentro de la carpeta src de este fichero raiz, un archivo llamado  "zzz.FechasMaximasAPI.js" donde se comprueba esto.

Con esto comprobado el filtro de fechas de ingreso y salida no modifica el renderizado de hoteles según este parametro de busqueda.

Sin embargo, sí modifica un texto dinámico como se aclaro anteriormente.

6. El botón Clear devuelve a los valores por defecto los dos filtros que funcionales en estre proyecto.

 -->
